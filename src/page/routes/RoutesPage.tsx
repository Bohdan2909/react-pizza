import React from 'react';
import {Route, Routes} from 'react-router-dom';

const RoutesPage = () => {
    return (
        <Routes>
            <Route path={'/'}/>
        </Routes>
    );
};

export default RoutesPage;